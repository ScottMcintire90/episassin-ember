import { deadFace } from 'episassin/helpers/dead-face';
import { module, test } from 'qunit';

module('Unit | Helper | dead face');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = deadFace([42]);
  assert.ok(result);
});
